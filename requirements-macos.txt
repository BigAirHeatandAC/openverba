# OpenVerba — macOS build/runtime deps (CI: build-macos job).
#
# The cross-platform core (same pins as requirements.txt) MINUS the Windows-only
# stack (pywin32 / keyboard / mouse), PLUS the macOS pyobjc + pynput backend deps
# used by voiceflow.platform.macos (NSPasteboard clipboard, Quartz/CGEvent paste,
# pynput + CGEventTap hotkeys, TCC permission probes).

# --- cross-platform core (speech + audio + GUI) ---
faster-whisper==1.2.1
ctranslate2==4.8.0
huggingface_hub==1.19.0
tokenizers==0.23.1
av==17.1.0
onnxruntime==1.26.0
numpy==2.4.6
sounddevice==0.5.5
pyperclip==1.11.0
customtkinter==5.2.2
darkdetect==0.8.0
pystray==0.19.5
pillow==12.2.0

# --- macOS input / clipboard / paste backend ---
pynput>=1.7
pyobjc-core>=10
pyobjc-framework-Cocoa>=10                 # Foundation, AppKit
pyobjc-framework-Quartz>=10                # Quartz / CoreGraphics (CGEvent Cmd+V)
pyobjc-framework-ApplicationServices>=10   # AX / pasteboard helpers
