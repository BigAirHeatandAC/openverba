# UTF-8
#
# VoiceFlow - Windows executable version resource.
# Consumed by PyInstaller (EXE(... version='version_info.txt' ...)) to stamp the
# generated VoiceFlow.exe with file/product version + company/description so the
# binary looks intentional in Explorer "Properties > Details" and to SmartScreen.
#
# Keep the four-tuple versions in sync with voiceflow.__version__ (1.0.0 -> 1,0,0,0).

VSVersionInfo(
  ffi=FixedFileInfo(
    # filevers / prodvers are (major, minor, patch, build).
    filevers=(1, 0, 0, 0),
    prodvers=(1, 0, 0, 0),
    # Contains a bitmask that specifies the valid bits 'flags'.
    mask=0x3f,
    flags=0x0,
    # The operating system for which this file was designed. 0x40004 = NT/Win32.
    OS=0x40004,
    # 0x1 = application.
    fileType=0x1,
    subtype=0x0,
    date=(0, 0)
  ),
  kids=[
    StringFileInfo(
      [
      StringTable(
        # 0409 = U.S. English, 04B0 = Unicode codepage (1200).
        u'040904B0',
        [StringStruct(u'CompanyName', u'VoiceFlow Project'),
        StringStruct(u'FileDescription', u'VoiceFlow - free, local, offline voice typing for Windows'),
        StringStruct(u'FileVersion', u'1.0.0.0'),
        StringStruct(u'InternalName', u'VoiceFlow'),
        StringStruct(u'LegalCopyright', u'MIT License. Copyright (c) 2026 VoiceFlow Project.'),
        StringStruct(u'OriginalFilename', u'VoiceFlow.exe'),
        StringStruct(u'ProductName', u'VoiceFlow'),
        StringStruct(u'ProductVersion', u'1.0.0.0')])
      ]),
    VarFileInfo([VarStruct(u'Translation', [0x409, 1200])])
  ]
)
